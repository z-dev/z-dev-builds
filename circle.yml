machine:
  node:
    version: 8.1.2

deployment:
  development:
    branch: master
    commands:
      - yarn run deploy:ci:dev
  production:
    branch: production
    commands:
      - yarn run deploy:ci:prod

